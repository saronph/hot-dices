import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  adContainer: {
    position: "absolute",
    bottom: 32,
    width: "100%",
    alignItems: "center",
    backgroundColor: "transparent",
  },
});
